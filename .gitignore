*.o
*.dSYM
__pycache__/
audio_state.ini
# ...but the repl_live_demo ships a default config (not runtime state).
!tools/repl_live_demo/repl_live_demo.ini
# The built-in example catalog is authored source, not runtime state.
!examples/catalog.ini
# repl_live_demo 'e' round-trip export artifacts (written to CWD).
*.roundtrip.c
*.roundtrip.glr
gl-repl
gl-repl-unchained
output.c
output.ply
output.glr
build/
# Vendored freeglut CMake build output (also matched by the bare build/ above;
# anchored here for clarity). build-osmesa/ is the FREEGLUT_OSMESA=1 variant's
# tree. The vendored *source* under third_party/freeglut/ is checked in; only
# its build*/ trees are generated.
third_party/freeglut/build*/
# gl4es + GLU wasm static libs and checkouts, fetched/built by
# scripts/web-deps.sh for the WEB=1 build.
third_party/web/
/workspaces/
/recovery-workspace/
bench_repl
/render3d_demo
/render3d_hot_demo
/repl_demo
/repl_live_demo
/editor_demo
/editor_demo.txt
/memprof_demo
/variable_panel_demo
/color_picker_demo
/assign_plot_demo
/cpuprof_demo
/render-3d-asset-builder
.cache/
compile_commands.json
coverage.lcov
callgraph*.mmd
callgraph*.dot
callgraph*.html
callgrind.out*
.compat-scratch/
*.swp
*.swo
*.DS_Store
my_scene.c
recovery.c

# macOS app-bundle build products (regenerate via `make app`)
/gl-repl.app/
/packaging/macos/gl-repl.icns
/packaging/macos/gl-repl.iconset/

# Release staging artifacts (regenerate via `make release`)
/dist/
# Per-user release build plan (hosts/modes) — edit via `make release-config`.
/.release.ini

# Recorded videos (regenerate via scripts/record-video.sh) — large files
# stay out of the repo.
/docs/images/*.mp4
