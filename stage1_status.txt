# Stage 1 Conversion Status Summary

## Exit Criteria Met ✓
1. g_repl_state is the sole storage owner
2. repl_state_capture()/repl_state_restore() exists and works
3. All 22 slices tested in capture/restore round-trip (59/59 tests pass)

## Current State: 14 Direct-Runtime, 8 Facade-Backed

### Direct-Runtime Slices (returning &g_repl_state.<slice>)
These have been converted to skip the facade entirely:
- selection ✓
- clipboard ✓
- help ✓
- variable_panel ✓
- variable_drag ✓
- profile_panel ✓
- status ✓
- search ✓
- autocomplete ✓
- camera ✓
- pointer ✓
- viewport ✓
- scenes ✓
- import_export ✓

### Facade-Backed Slices (returning &g_repl_facade.<slice>)
These still use the facade but storage is in g_repl_state:
- document (large array, high-impact)
- flat_program (large array, high-impact)
- variables (arrays, medium-impact)
- editor_input (buffers, medium-impact)
- code_panel (medium-impact)
- presentation (many toggle fields, large)
- render (lights array, medium)
- replay (medium-impact)

## Stage 1 Completion Strategy

The capture/restore mechanism works for all 22 slices because it directly 
copies g_repl_state, bypassing facades. So Stage 1 is functionally complete.

To formally complete Stage 1:
1. Document the facade-backed vs direct-runtime status ✓ (this file)
2. Ensure facade initialization builds correct pointers to g_repl_state fields
3. Verify no stale facades interfere with capture/restore
4. Mark Stage 1 as complete with clear commit message

## Next Steps (Stage 2+)
- Convert remaining facade-backed slices to direct-runtime (option 1)
- Replace pointer access patterns with value-getter functions (option 2)
- This work is deferred and tracked separately from Stage 1
