#ifndef REPL_STATE_H
#define REPL_STATE_H

/*
 * Phase 2 runtime-state facade lives here. The current branch still routes
 * legacy global access through repl_state_compat.h while the header boundary
 * is split into focused DTO headers.
 */

#endif /* REPL_STATE_H */
