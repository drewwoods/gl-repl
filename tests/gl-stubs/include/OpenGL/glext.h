#ifndef STUB_OPENGL_GLEXT_H
#define STUB_OPENGL_GLEXT_H

/* Compatibility wrapper for the Apple-GLUT include path: everything the
 * sample uses from glext.h (the timer-query getter) lives in the GL/gl.h
 * stub. */
#include <GL/gl.h>

#endif
