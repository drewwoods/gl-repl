TODO:
   - ability to switch between 2D orth where use glVertex2d
   - when glEnable(GL_LIGHTING) is commented I expected the lighting to be disabled, since that is the default state. 
   - the exported C code sets the same windows size (or at least window aspect so the same clipping is applied)
   - Have a 2d Mode, that swings the camera so looking down the z axis and engages the Adaptive plans grid.  Also the mouse click and drag turns to a translation along the z=0 plane instead of camera orbit
   - the status bar should restore the color - means if the user application didnt get color it picks up the status bar color
   - glPoints should have their point enlarged and colored when you are on a glVertex2f source line. glVertex3f works for coloring, but not point enlarged (maybe thats okay)
   - Using the status bar to enter a new filename is not ideal as it disappears
   - remove emdash — from status bar output since not ascii
   - See status bar history
   - add the light setup to the REPL (static) so the user can see the location and color of each
   - Think of how to handle music - maybe a menu?
   - Have a tutorial or popup windows
   - outlines dont function correctly when not using lighting
     - replace the exported code with a stencil test (color and depth mask false, stencil set, then color black where stencil set)
   - add ability to reset the x,y,z translation of the camera
   - add record once state is fully captured in a frameState
   - show user scene when currently selected
   - address the limitation of only 8 scope visible globals


• The limitation was the REPL’s local variable visibility cap, not the names themselves.

  Inside the loop body, the evaluator needs all visible variables:

  i
  angle
  ripplephase
  ripplewidth
  rippledelta
  rippleamp
  samples
  ringx
  ringy

  That is 9 visible variables: the loop variable i plus the 8 declared locals. The REPL’s local expression variable buffer is capped at MAX_EXPR_VARS, which appears to be 8 in this code path. When the example
  was exported to C and imported back, the import/normalization path only preserved the first 8 visible variables, so ringy fell out of scope.

  That made this line fail during import:

  glVertex3f(ringx, ringy, 0);

  The importer could still see ringx, but not ringy, so it warned that it could not parse the line and dropped part of the example. That caused the example round-trip test to fail.

  So the fix was to reduce the number of simultaneously visible variables. I removed angle and samples by inlining their expressions and kept the more meaningful ripple names:

  ripplephase
  ripplewidth
  rippledelta
  rippleamp
  x
  y

  Inside the loop that gives 7 visible variables including i, which stays under the cap and round-trips cleanly.

