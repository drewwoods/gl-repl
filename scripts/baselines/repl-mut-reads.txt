# Baseline for scripts/check-repl-mut-reads.sh.
#
# Counts `_mut(` call sites in src/repl/*.c outside the owner files
# (state.c, apply.c, command_store.c, eval.c). The ratchet prevents
# the count from growing as a guard against re-introducing
# `_mut()`-for-reads — the audit #7/#14 anti-pattern where pure
# reads went through the mutator accessor and defeated the const
# contract of the typed-state facade.
#
# After the 2026-06-08 sweep (Final Elimination):
#  - example_loader.c: active_example_idx write -> setter (1)
#  - export.c / import.c: IMPORT_EXPORT_STATE macro -> read/write split (2)
#  - executor.c: EXEC_RENDER macro -> render setters (1)
#  - flatten.c: flat_program_mut -> flat_program_writable (1)
#  - scenes.c: SCENE_STATE/IMPORT_EXPORT_STATE macros -> read/write split (2)
#  - scenes.c: scene_cfg_mut / pre_example_cfg_mut -> cfg_writable (4)
#  ── total: remaining 11 sites moved to explicit write surfaces.
#
# NEW RULE: ZERO `_mut()` calls allowed outside owner files.
mut_call_count: 0
