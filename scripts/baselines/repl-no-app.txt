# Baseline for scripts/check-repl-no-app.sh.
#
# Counts non-comment `#include "app/...` lines in src/repl/**.
# The REPL program-model layer must not depend on the app shell.
#
# Closed 2026-05-26 (audit #1): the previous violation —
# src/repl/help_text.c including app/glr_config.h to walk
# g_cfg_items[] for the F-key rows — was removed by routing F-key
# label lookups through a controller-installed ReplHelpFkeyProvider
# (see src/repl/help_text.h and src/app/glr_ctrl.c's
# glr_ctrl_help_fkey_label). The baseline drops from 1 to 0; any
# new app/ include from src/repl/ is now a hard error.

app_include_count: 0
