# Editor / UI ownership transitional-coupling budget.
#
# Tracked by scripts/check/check-editor-ownership-budget.sh. Both signals
# ratchet down only — a commit cannot grow either count past these
# baselines. Reductions are always allowed; ratchet by hand.
#
# Targets (Phase 4 / Phase 5):
#   ui_forwarder_count -> 0  (UiAction eliminates the call sites)
#   ui_state_includes_repl_views -> 0  (typedefs migrate to a ui-owned
#     header)
#
# History: the forwarder block in repl_state.c was deleted in Phase A
# commit 14; the ratchet then sat at 3 to admit three legitimate
# non-forwarder `ui_state_*()` calls (the reset_all sweep and two
# auto_rotate writes through ui_state_camera_mut). Those calls have
# since been moved through ui-owned paths and the grep counts zero —
# baseline tightened to 0 on 2026-05-27 so any reintroduction fails CI.
#
# The Phase 5 typedef migration landed: the UI-chrome typedefs
# (UiCodePanelRuntimeState / UiHelpState / UiProfilePanelState /
# UiStatusState / GlrCameraState / UiPointerState / UiViewportState)
# moved from repl_state_views.h to src/ui/app/state_types.h, and
# src/ui/app/state.h includes the ui-owned header directly. The
# variable-panel snapshot view now lives with the variable-panel peer as
# VariablePanelViewState. The ratchet target reached 0.

ui_forwarder_count: 0
ui_state_includes_repl_views: 0
