# Baseline for scripts/check/check-editor-no-app.sh.
#
# Counts non-comment `#include "app/...` lines in src/editor/**.
# The editor (layer 2) should not depend on the controller (layer 6).
# Audit finding #8 in docs/plans/in-review/src-editor-code-smell-audit.md
# previously documented four violations in src/editor/input.c. The
# week-pass fix landed 2026-05-24:
#   - editor_reset_transients moved to src/app/glr_ctrl.c as
#     glr_ctrl_reset_transients
#   - editor_input_restore_hidden_code_panel moved as
#     glr_ctrl_restore_hidden_code_panel; the editor signals via
#     the restore_hidden_code_panel effect flag, the controller
#     actualizes it from glr_ctrl_apply_input_effects
#   - editor_input_code_panel_layout reads through the new
#     EditorCodePanelLayoutProvider hook (production installs
#     glr_ctrl_code_panel_layout_provider at glr_ctrl_init_gl;
#     tests install it in main())
#   - EditorCompletionProvider gained an `accept` callback so the
#     Tab/Enter routes use editor_completion_accept() instead of
#     glr_completion_accept_autocomplete directly (audit #9)
# All four app/ includes are gone. Hard zero from here.
#
# Recorded 2026-05-24 after the audit #8/#9 hoist landed.

app_include_count: 0
