# Baseline for scripts/check-editor-no-app.sh.
#
# Counts non-comment `#include "app/...` lines in src/editor/**.
# The editor (layer 2) should not depend on the controller (layer 6).
# Audit finding #8 in plans/in-review/src-editor-code-smell-audit.md
# documents the four violations in src/editor/input.c (`app/glr_completion.h`,
# `app/glr_state.h`, `app/glr_camera.h`, `app/glr_ctrl.h`). The
# week-pass fix hoists `editor_reset_transients` and
# `editor_input_restore_hidden_code_panel` into src/app/glr_ctrl.c
# and adds `accept` to EditorCompletionProvider, which drops all
# four; ratchet down then.
#
# Recorded 2026-05-24 after the mechanical audit-fix pass landed.

app_include_count: 4
