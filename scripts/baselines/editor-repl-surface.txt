# Baseline for scripts/check-editor-repl-surface.sh.
#
# Counts unique `repl_*(` symbols called from each editor file.
# These are the direct REPL coupling that tools/editor_demo/repl_shim.c
# used to have to stub (the shim was deleted in Phase 5 of
# plans/done/edit-line-ownership.md after the storage flip removed
# its last forwarders). Subsequent phases ratchet this surface down
# by consolidating or deleting direct editor -> REPL dependencies.
#
# Recorded 2026-05-21 (post Phase 5 of edit-line-ownership.md — the
# storage flip dropped input.c from 22→20 and commit.c from 30→28
# by removing repl_state_edit_line / _set references).
#
# 2026-05-21: bumped both by +1 to admit repl_set_status_error, the
# error sibling of repl_set_status (which is already in the surface).
# A future shared status surface could let both drop by 2, but that
# refactor is independent of the red-status work that introduced the
# new symbol.
#
# 2026-05-24: input_c bumped +1 to admit repl_canonical_input_view,
# the consolidated "skip leading ws + drop trailing ';'/ws" helper
# from src/repl/text_helpers.c. Replaces three open-coded variants
# of the same strip in input.c per audit finding #19 — net code
# reduction, but a new repl_* symbol.
#
# 2026-05-25: input_c bumped +1 for repl_eval_is_ident_continue
# (audit #45, shared ident predicate replacing local is_word_char).
# commit_c bumped +3: repl_eval_is_ident_start, _continue (audit #45),
# and repl_parser_parse_command_ctx (audit #26 — replaced the last
# EditorServices.parse_command_ctx indirection with a direct call,
# eliminating commit.c's services.h include entirely).
#
# 2026-05-26: commit_c bumped +3 for repl_eval_numeric_arg_at_cursor,
# repl_eval_swatch_step, and repl_eval_format_swatch_number (Finding #41,
# moving swatch commit logic from the controller to commit.c).
# 2026-05-26: input_c bumped +1 for repl_parser_parse_command_ctx
# (Finding #39, replacing the last input-side parse dispatch shim and
# deleting src/editor/services.{c,h}).
#
# 2026-05-27: commit_c tightened 35 -> 27. The dedup work in commits
# 9fa578f / 7cb010c / 9479d3a / and the four kernel-extraction
# commits closing #15 retired eight direct repl_* surface symbols
# (swatch helpers were consolidated; the cfg_baseline bridge ate
# what was direct call sites). No commit since had to expand it, so
# the baseline is brought to the new low.
#
# 2026-05-29: input_c bumped +1 for repl_source_scope_matrix_scope_depth_at,
# the matrix-stack sibling of repl_source_scope_in_begin_block_at and
# repl_source_scope_block_depth_at already called in the same
# rewrite_source_text_with_indent helper. glPushMatrix/glPopMatrix now
# open an indent scope like glBegin/glEnd, so the manual indent rewriter
# (used for var-bearing body lines) must read matrix depth to match the
# parser's canonical indent. No new coupling kind — same source_scope
# depth-query family already in the file.
#
# Ratchet down as migrations land. The plan target
# (plans/done/editor-demo.md) is ~5 per file.

input_c: 25
commit_c: 27
