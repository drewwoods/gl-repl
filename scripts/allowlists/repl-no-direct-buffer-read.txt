# Files allowed to call editor_buffer_line() directly (relative path,
# matched against the `file:` prefix of a hit).
#
# Empty: the REPL pipeline (src/repl/*.c) reaches editor text only
# through the source_document port / EditorBufferView parameters, so
# no src/repl file calls editor_buffer_line() directly. The historical
# entries (repl_editor.c, repl_code_panel_document.c) were deleted in
# the editor-text-model split and the src/ restructure; this guard now
# scans src/repl/*.c and the allowlist stays empty.
