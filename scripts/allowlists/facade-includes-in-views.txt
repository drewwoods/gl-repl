# View files (src/ui, src/render3d) allowed to include a repl/state*.h
# header directly. Entries are repo-relative paths matched against the
# `file:` prefix of a hit. Re-baselined to current reality after the
# src/ restructure (the guard now scans src/ui + src/render3d; the old
# flat-layout entries ui_color_picker.c / ui_help_overlay.c are gone,
# and the files that no longer include the header were pruned).
#
# All three remaining includes are repl/state_views.h (the read-only
# value-type header), pulled in to *name* the Repl*State value types,
# not to reach a mutator facade:
#   - snapshot.h        — the unified UI render snapshot embeds the
#                         Repl*State value types (snapshot is the read
#                         boundary, not a per-renderer facade).
#   - menu_bar.c        — reads Repl*State value fields off the snapshot.
#   - repl_code_panel.c — the REPL-aware code-panel adapter names the
#                         same value types.
src/ui/app/snapshot.h
src/ui/app/menu_bar.c
src/ui/app/repl_code_panel.c
