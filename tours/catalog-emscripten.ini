# Emscripten/web guided-tour catalog. Selected by Makefile when WEB=1.
# Keep section names and display names aligned with catalog.ini where the
# tour exists on both platforms. Web-specific scripts may target shell: DOM
# controls that replace hidden native menus (for example shell:new).

[menus-and-examples]
file = menus-and-examples.pointer
name = Menus & Examples

[editing-basics]
file = editing-basics-emscripten.pointer
name = Editing Basics

[camera-and-views]
file = camera-and-views.pointer
name = Camera & Views

[getting-help]
file = getting-help.pointer
name = Getting Help
