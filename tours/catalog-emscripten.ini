# Emscripten/web guided-tour catalog. Selected by Makefile when WEB=1.
# Keep section names, display names, and files aligned with catalog.ini where
# the tour exists on both platforms. Shared .pointer files can isolate
# shell: DOM controls that replace hidden native menus (for example shell:new)
# with #ifdef __EMSCRIPTEN__.

[start-here]
file = start-here.pointer
name = Start Here

[editing-basics]
file = editing-basics.pointer
name = Editing Basics

[camera-and-views]
file = camera-and-views.pointer
name = Camera & Views

[overlays-and-scope]
file = overlays-and-scope.pointer
name = Overlays & Scope

[getting-help]
file = getting-help.pointer
name = Getting Help
